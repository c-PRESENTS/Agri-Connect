My website is only looking correct when I manually switch the browser into mobile emulation mode. In the normal real view on phones, it is not behaving like a true responsive mobile website. I need you to fix this properly.

Main problem:
The site should automatically render correctly on real mobile devices without me changing browser settings. It should behave like a proper mobile-first site, similar to major e-commerce sites, with correct viewport behaviour, responsive layout, and no desktop-width rendering on phones.

Strict instructions:
- Audit the current app first and identify why the layout only looks right in forced mobile emulation
- Check the HTML head and viewport configuration first
- Fix the viewport setup so the site uses the real device width properly
- Then audit the CSS and layout structure for fixed widths, overflow, desktop-first layout issues, and broken breakpoints
- Rebuild any sections that still behave like desktop layouts on small screens
- Do not only resize elements
- Do not stop at visual tweaks
- Make the site truly responsive on actual mobile devices

Mandatory fixes:
1. Ensure the correct viewport meta tag exists in the main HTML head
2. Ensure the viewport is configured for proper device-width rendering
3. Remove or refactor any fixed-width layout causing desktop-style rendering on phones
4. Replace rigid widths with fluid responsive sizing
5. Fix breakpoints so the mobile layout activates automatically on real devices
6. Ensure the home page renders correctly on common phone widths without manual browser changes
7. Ensure login, profile, search, product pages, cart, checkout, and support pages also render correctly on real mobile screens
8. Remove horizontal overflow
9. Prevent clipped text and overlapping elements
10. Make header, search, cards, map, and navigation adapt properly to mobile width
11. Keep desktop layout working after mobile fixes

Technical expectations:
- Check the main layout containers, CSS rules, media queries, viewport tag, and responsive structure
- Fix any use of fixed pixel widths that break mobile layout
- Ensure major sections use width: 100%, max-width, flex-wrap, grid, and proper responsive spacing where appropriate
- Make mobile-first styles the default, then add larger breakpoints for tablet and desktop
- Test around 375px, 390px, 768px, and desktop widths
- Ensure the result works in a normal mobile browser without using device emulation mode

Before implementing:
1. Tell me whether the viewport meta tag is missing or incorrect
2. Tell me which layout sections are still desktop-width based
3. Tell me what needs restructuring
4. Then fix the site section by section

Do not finish until:
- The site renders properly on a real mobile viewport without manual browser emulation
- The home page works correctly on mobile
- The top navigation is usable
- The search bar fits correctly
- Cards and content stack properly
- No section looks like a shrunk desktop layout
- No horizontal overflow exists on the main pages