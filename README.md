# AgriConnect

Project documentation lives in [docs/README.md](docs/README.md).

## Quick Start

```bash
npm install
npm run db:push
npm run dev
```

The application is organized into `frontend/`, `backend/`, `shared/`, `database/`, `scripts/`, and `docs/`.
